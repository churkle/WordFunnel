#pragma once
// Generates Word Funnels from a file that contains a list of words separated by newlines

#include <string>;

class WordFunnelGenerator
{
	public WordFunnelGenerator(std::string filePathName);
};